# === HTTP ===
quarkus.http.port=8081
quarkus.http.host=0.0.0.0

# === Database Primary (Write) ===
quarkus.datasource.db-kind=postgresql
quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5432/econ
quarkus.datasource.username=game
quarkus.datasource.password=gamepass

# === Database Replica 1 (Read) ===
quarkus.datasource."replica1".db-kind=postgresql
quarkus.datasource."replica1".jdbc.url=jdbc:postgresql://localhost:5433/econ
quarkus.datasource."replica1".username=game
quarkus.datasource."replica1".password=gamepass

# === Database Replica 2 (Read) ===
quarkus.datasource."replica2".db-kind=postgresql
quarkus.datasource."replica2".jdbc.url=jdbc:postgresql://localhost:5434/econ
quarkus.datasource."replica2".username=game
quarkus.datasource."replica2".password=gamepass

# === Flyway (только для primary) ===
quarkus.flyway.migrate-at-start=true
quarkus.flyway.baseline-on-migrate=true
quarkus.flyway.baseline-version=1

# === Hibernate ===
quarkus.hibernate-orm.database.generation=validate
quarkus.hibernate-orm.log.sql=true

# === Redis (quarkus-redis-client) ===
quarkus.redis.hosts=redis://localhost:6379

# === NATS / JetStream (Quarkus Ð½Ð° Ð¥ÐÐ¡Ð¢Ð) ===
app.nats.servers=nats://localhost:4222
app.nats.name=econ-service

# Ð¡ÑÑÐ¸Ð¼ Ð¸ subject Ñ Ð¿ÐµÑÑÐ¸ÑÑÐµÐ½ÑÐ½Ð¾ÑÑÑÑ
app.nats.js.stream=ECON
app.nats.js.subject=econ.production.done
# ÐÑÐ»Ð¸ ÑÐ¾ÑÐµÑÑ Ð»Ð¾Ð²Ð¸ÑÑ Ð½ÐµÑÐºÐ¾Ð»ÑÐºÐ¾ ÑÐµÐ¼: econ.production.* (Ð¸ Ð¿ÑÐ±Ð»Ð¸ÐºÐ¾Ð²Ð°ÑÑ Ð² done/failed/started)
# app.nats.js.subject=econ.production.*

# === Scheduler ===
quarkus.scheduler.enabled=true

# === Health / Metrics / Logging ===
quarkus.smallrye-health.root-path=/health
quarkus.smallrye-health.liveness-path=/live
quarkus.smallrye-health.readiness-path=/ready
quarkus.smallrye-metrics.path=/metrics

quarkus.log.level=INFO
quarkus.log.category."com.example.economy".level=DEBUG

# === JWT Security for Admin Panel ===
smallrye.jwt.sign.key=MIIEvgIBADANBgkqhkiG9w0BAQEFAASCBKgwggSkAgEAAoIBAQDTG8ilQ4GZuQ5N
mp.jwt.verify.issuer=https://economy-admin
quarkus.smallrye-jwt.enabled=true

# === Admin Users (Properties File Security) ===
quarkus.security.file.enabled=true
quarkus.security.file.users=admin=admin123,developer=dev456
quarkus.security.file.roles=admin=admin,developer=developer
quarkus.security.file.realm-name=admin-realm
quarkus.security.file.plain-text=true

# === HTTP Basic Auth ===
quarkus.http.auth.basic=true

# === CORS для веб-интерфейса ===
quarkus.http.cors=true
quarkus.http.cors.origins=http://localhost:3000,http://localhost:8081
quarkus.http.cors.methods=GET,POST,PUT,DELETE,OPTIONS
quarkus.http.cors.headers=accept,authorization,content-type,x-requested-with

# === Static Resources ===
# Статические ресурсы будут доступны по /admin/ из META-INF/resources/admin/



